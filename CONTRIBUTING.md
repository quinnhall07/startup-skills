# Contributing

## Reporting issues
Open a GitHub issue. Include the skill name, what you said, and what the system did vs. what you expected.

## Pull requests
- Skills must be under 250 lines
- References must cite named sources inline
- All changes validated with `claude plugin validate plugins/startup-skills` before submitting
- Bump the version in `plugin.json` and add a CHANGELOG entry